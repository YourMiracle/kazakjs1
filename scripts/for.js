
var numbers = [1, 2, 3, 4, 5];
var words = ['c', 'u', 'c', 'l','e', 's'];
var forYou = [];

for (var i = 0; i < numbers.length; i++){
    var arrayElem = numbers[i];
    for (var j = 0; j < words.length; i++) {
        if (words[j] = arrayElem) {
            forYou.push(words[j]);
            console.log(arrayElem)
        }
    }
}
